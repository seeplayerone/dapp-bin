pragma solidity 0.4.25;

import "github.com/evilcc2018/dapp-bin/library/template.sol";

contract FakePerson is Template {
    function() public payable {}
}

contract testArray {
    function lala() public returns (uint8) {
        uint8
        return list2.length;
    }
}